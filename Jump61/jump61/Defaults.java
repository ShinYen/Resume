package jump61;


/** Collected constants used as default or initial values.
 *  @author Shin-Yen Huang
 */
class Defaults {

    /** Current version designator. */
    static final String VERSION = "Jump61 2.0";

    /** Default board size. */
    static final int BOARD_SIZE = 6;

    /**Default spots value. */
    static final int SPOT_VAL = 100;

    /** Default col val. */
    static final int COL_VAL = 200;

    /** Default col neigh val. */
    static final int NEIGH_VAL = 300;

    /** Default Negative Bound. */
    static final int NEG_BOUND = -100000;

    /** Default Top Bound. */
    static final int POS_BOUND = 100000;

    /** Window width. */
    static final int FRAME_WIDTH = 600;

    /** Window length. */
    static final int FRAME_LEN = 600;

    /** Status bar Len. */
    static final int STATUS_LEN = 16;

    /** Menu bar len. */
    static final int MENU_LEN = 23;

    /** Circle size. */
    static final int CIRC_SIZE = 10;

    /** mid ind of die. */
    static final int MID_DIE = 40;

    /** top ind of die. */
    static final int TOP_DIE = 10;

    /**right ind of die. */
    static final int RIGHT_DIE = 70;

    /** panel size. */
    static final int PANEL_SIZE = 100;

    /** dimensions. */
    static final int DIM_A = 170;

    /** dimensions. */
    static final int DIM_B = 200;

    /** dimensions. */
    static final int DIM_C = 150;
}
